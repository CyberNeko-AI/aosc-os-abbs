# Downstream kernels often rebases upon upstream releases.
# For use inside autobuild/
__VER=6.12.34
RPI_TAG=stable_20250702
# Use this for stable releases.
VER="${__VER}+${RPI_TAG/_/}"

SRCS="git::commit=tags/${RPI_TAG}::https://github.com/raspberrypi/linux.git"
CHKSUMS="SKIP"
